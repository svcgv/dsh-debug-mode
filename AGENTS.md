# AGENTS.md

本文件是 `dsh-debug-mode` 仓库的最高优先级工程规范。项目用于扩展 DeepSeek Harness，任何实现都必须优先保证用户源码、运行进程和调试数据的可恢复性。

## 项目目标

构建一个可独立安装的 DeepSeek Harness Debug Mode Bundle。插件在 Web composer 中提供 Normal、Plan、Debug 三种互斥模式，并为前端运行时埋点和 Node.js/Python 后端调试提供统一工作流。

阶段一只建立规范、工程骨架和自动化门禁。未得到用户对阶段一结果的明确确认前，不得实现 Debug Mode 业务行为。

## 开发规则

1. 先检查现有代码、配置和运行状态，再修改。不得询问可以从仓库或系统发现的事实。
2. 一次变更只解决一个明确问题。公共行为、类型、文档和测试必须在同一变更中保持一致。
3. 禁止静默降级、静默捕获异常或伪造成功。错误必须说明失败阶段、影响、稳定错误码和恢复方法。
4. 用户源码发生版本漂移时，禁止用旧备份整文件覆盖。只能删除能够证明属于当前运行的生成节点；无法证明时保留文件并报告。
5. 只有插件自己启动的进程可以直接结束。结束已存在的后端服务前，必须展示 PID、命令、工作目录、端口和恢复命令并取得用户确认。
6. 日志、错误和工具结果不得泄露 token、password、secret、authorization、cookie、key 等敏感值。
7. 所有循环、队列、网络请求、日志、递归、等待和子进程都必须有明确上限、超时、取消和清理策略。
8. 所有跨平台差异必须封装在 Adapter 内，不得在业务模块中散布 `process.platform` 判断。
9. 每个异步资源只有一个生命周期所有者。启动、取消、失败、完成和插件卸载都必须收敛到同一清理路径。
10. 不得提交密钥、运行日志、调试备份、构建产物、临时 profile 或本机绝对路径。

## 代码约定

- Node.js `>=22.19.0`，pnpm `11.x`，TypeScript strict，ECMAScript modules。
- 2 空格缩进、单引号、无分号、LF、UTF-8、文件末尾一个换行。
- 标识符、类型、错误码、JSDoc、代码注释和提交信息使用英文；面向项目维护者的规范文档使用中文。
- 禁止 `any`、无说明的类型断言、`@ts-ignore`、浮动 Promise 和无界输出。
- 公共导出必须有英文 JSDoc。注释解释约束、所有权和失败语义，不复述代码。
- 模块应保持深接口：复杂实现隐藏在少量稳定操作之后。测试和调用方通过同一接口交互。

## 模块 seam

- `DebugRunManager`：每会话一个活动运行，负责状态与资源所有权。
- `SourceAdapter`：解析、插入、验证和移除语言特定探针。
- `DebuggerAdapter`：启动、断点、暂停、检查和 stepping。
- `ProcessAdapter`：进程发现、端口映射、进程树结束和恢复。
- `TraceStore`：有界事件写入、游标读取、脱敏和轮转。

只有存在真实变体时才引入 Adapter。JS/TS、Vue/Svelte、Dart；Node/Python；macOS/Linux/Windows 分别构成真实变体。

## 测试与门禁

- 状态机、源码变换、清理恢复、监听协议、调试协议、进程结束和崩溃恢复必须达到 100% 语句、分支、函数和行覆盖率。
- 全项目语句、函数和行覆盖率不得低于 90%，分支不得低于 85%。
- 测试必须拥有并清理自己的临时目录、端口、计时器和子进程，能够并发执行。
- 修改后运行与范围匹配的测试；交付阶段一或发布前运行 `pnpm check`。
- 不得通过无意义断言、忽略指令或排除真实业务文件绕过覆盖率。

## Git 约定

- 分支名称优先使用 `codex/` 前缀。
- 提交采用 Conventional Commits，例如 `docs: define project standards`。
- 不修改、重写或删除用户已有提交；未经明确要求不执行 push、force push、reset hard 或 clean。
- 提交前必须通过 Husky pre-commit；推送前必须通过 pre-push。

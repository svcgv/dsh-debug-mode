/** Host plugin name reserved for the phase-two implementation. */
export const name = 'dsh-debug-mode'

/** Stage-one no-op Host entry. Debug behavior is intentionally not implemented yet. */
export function apply(): void {}

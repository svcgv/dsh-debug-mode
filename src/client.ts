/** Browser plugin name reserved for the phase-two implementation. */
export const name = 'dsh-debug-mode-client'

/** Stage-one no-op Browser entry. The ui-plan slot remains untouched. */
export function apply(): void {}
